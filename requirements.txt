# =============================================================================
# DeepCache – Python dependencies
# Verified on: Google Colab (Python 3.12, CUDA 12.8, GPU: T4/A100)
# Install:  pip install -r requirements.txt && pip install -e .
# =============================================================================

# -----------------------------------------------------------------------------
# Core deep-learning stack
# Note: Colab ships torch 2.10.0+cu128 / torchvision 0.25.0+cu128.
#       The +cuXXX suffix is CUDA-specific and cannot be pinned in a plain
#       requirements.txt; pip will resolve the correct wheel for your hardware.
# -----------------------------------------------------------------------------
torch==2.10.0
torchvision==0.25.0

# -----------------------------------------------------------------------------
# Diffusion-model libraries  (exact versions from Colab run)
# -----------------------------------------------------------------------------
diffusers==0.37.1
transformers==5.0.0
accelerate==1.13.0
safetensors==0.7.0
tokenizers==0.22.2
huggingface-hub==1.11.0

# -----------------------------------------------------------------------------
# Numerical / image utilities  (exact versions from Colab run)
# -----------------------------------------------------------------------------
numpy==2.0.2
Pillow==11.3.0
tqdm==4.67.3
scipy==1.13.1

# -----------------------------------------------------------------------------
# Evaluation metrics
# -----------------------------------------------------------------------------
open_clip_torch==2.26.1
lpips==0.1.4
pytorch_fid==0.3.0

# -----------------------------------------------------------------------------
# Data loading
# -----------------------------------------------------------------------------
datasets==2.21.0
lmdb==1.5.1

# -----------------------------------------------------------------------------
# Web demo
# -----------------------------------------------------------------------------
streamlit==1.57.0
